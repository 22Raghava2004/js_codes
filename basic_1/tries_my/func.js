function abechut(a,gajma)
{
    for (a;a<20;a++)
    {
        if (a==15)
        {console.log(gajma)
        dj=prompt('enter ur name')

        if (dj.startsWith('r')){
            console.log('u are genius')
        }
        else{console.log('shut the f up')}
    }
    console.log(`bas bahut hogaya ${gajma}`)
    }}

gajma="onicha"
abechut(20,gajma)